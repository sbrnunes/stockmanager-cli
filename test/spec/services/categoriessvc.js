'use strict';

describe('Service: CategoriesSvc', function () {

  // load the service's module
  beforeEach(module('stockmanagerApp'));

  // instantiate service
  var CategoriesSvc;
  beforeEach(inject(function (_CategoriesSvc_) {
    CategoriesSvc = _CategoriesSvc_;
  }));

  //it('should do something', function () {
  //  expect(!!CategoriesSvc).toBe(true);
  //});

});
